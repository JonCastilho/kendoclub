# Nuxt
.nuxt
.nitro
.output
.data
.cache
dist

# Node
node_modules
*.log
# O .npmrc do projeto é versionado (ver o comentário dentro dele).
# Nunca coloque credenciais de registry nele — use o ~/.npmrc pessoal.

# Ambiente — nunca versionar credenciais
.env
.env.*
!.env.example

# Uploads locais (imagens de posts e eventos)
/uploads

# Editor e sistema operacional
.DS_Store
Thumbs.db
.idea
.vscode/*
!.vscode/extensions.json
